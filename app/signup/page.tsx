import { AuthPage2 } from "@/components/auth-sigup";

/**
 * Renders the signup demo page by displaying the AuthPage2 component.
 *
 * @returns The React element for the signup page (renders `AuthPage2`).
 */
export default function DemoPage() {
    return <AuthPage2 />;
}
