import { AuthPage } from "@/components/auth";

/**
 * Renders the shared authentication page.
 *
 * @returns The React element for the authentication page.
 */
export default function DemoPage() {
	return <AuthPage />;
}
